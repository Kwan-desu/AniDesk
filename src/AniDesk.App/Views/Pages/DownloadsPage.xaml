<UserControl
    x:Class="AniDesk.App.Views.Pages.DownloadsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:converters="clr-namespace:AniDesk.App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:AniDesk.App.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance vm:DownloadsViewModel}">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Downloads Header -->
        <Border Grid.Row="0" Background="#B311151F" BorderBrush="#1F283C" BorderThickness="0,0,0,1" Padding="24,14">
            <DockPanel LastChildFill="False">
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="Downloads &amp; Local Wallpapers" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" />
                    <TextBlock Text="Quickly change your desktop or lock screen to any downloaded wallpaper." FontSize="11.5" Foreground="#9DA6B8" />
                    <!-- Folder path row -->
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <ui:SymbolIcon Symbol="Folder24" FontSize="12" Foreground="#64748B" Margin="0,0,5,0" VerticalAlignment="Center" />
                        <TextBlock
                            Text="{Binding CurrentDownloadFolderPath}"
                            FontSize="11"
                            Foreground="#64748B"
                            VerticalAlignment="Center"
                            TextTrimming="CharacterEllipsis"
                            MaxWidth="420" />
                        <Border
                            Background="#1438BDF8"
                            BorderBrush="#2638BDF8"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="8,2"
                            Margin="10,0,0,0"
                            VerticalAlignment="Center">
                            <TextBlock Text="{Binding WallpaperCountText}" FontSize="10.5" Foreground="#38BDF8" FontWeight="SemiBold" />
                        </Border>
                    </StackPanel>
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button
                        Command="{Binding RefreshDownloadsCommand}"
                        Width="36" Height="34"
                        Margin="0,0,8,0"
                        Padding="0"
                        Background="#1F2535"
                        BorderBrush="#2C3446"
                        Foreground="#38BDF8"
                        ToolTip="Refresh Downloads">
                        <ui:SymbolIcon Symbol="ArrowClockwise24" FontSize="14" />
                    </Button>

                    <Button
                        Command="{Binding OpenDownloadFolderCommand}"
                        Height="34"
                        Background="#1F2535"
                        BorderBrush="#2C3446"
                        Foreground="#FFFFFF">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="FolderOpen24" Margin="0,0,6,0" FontSize="14" />
                            <TextBlock Text="Open Folder" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Downloads Content -->
        <ScrollViewer Grid.Row="1" Padding="24,16">
            <StackPanel>
                <!-- Active Downloads Queue (if any) -->
                <TextBlock Text="ACTIVE DOWNLOADS" FontSize="10.5" FontWeight="Bold" Foreground="#656E82" Margin="0,0,0,8"
                           Visibility="{Binding HasActiveDownloads, Converter={StaticResource BoolToVis}}" />

                <ItemsControl ItemsSource="{Binding Downloads}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Background="#161B27"
                                BorderBrush="#222838"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="14,10"
                                Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Thumbnail Preview -->
                                    <Border
                                        Grid.Column="0"
                                        Width="64"
                                        Height="40"
                                        CornerRadius="4"
                                        Background="#10141D"
                                        ClipToBounds="True"
                                        Margin="0,0,14,0">
                                        <Image converters:AsyncImageLoader.ImageUrl="{Binding Post.ThumbnailUrl}" Stretch="UniformToFill" />
                                    </Border>

                                    <!-- Info & Progress -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding FileName}" FontSize="12.5" FontWeight="SemiBold" Foreground="#FFFFFF" TextTrimming="CharacterEllipsis" />
                                        <TextBlock FontSize="11" Foreground="#9DA6B8" Margin="0,2,0,0">
                                            <Run Text="{Binding Post.FormattedDimensions}" />
                                            <Run Text=" • " />
                                            <Run Text="{Binding Status}" />
                                        </TextBlock>
                                        <ProgressBar
                                            Value="{Binding Progress}"
                                            Maximum="100"
                                            Height="3"
                                            Margin="0,4,0,0"
                                            Visibility="{Binding Status, Converter={StaticResource EqualityToVis}, ConverterParameter='Downloading'}" />
                                    </StackPanel>

                                    <!-- Action -->
                                    <Button
                                        Grid.Column="2"
                                        Command="{Binding DataContext.OpenFileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding TargetFilePath}"
                                        Background="#202636"
                                        BorderBrush="#2C3446"
                                        Foreground="#E2E8F0"
                                        FontSize="11.5"
                                        Content="Open File" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Downloaded Wallpapers Section Header -->
                <TextBlock Text="DOWNLOADED WALLPAPERS" FontSize="10.5" FontWeight="Bold" Foreground="#656E82" Margin="0,12,0,8" />

                <!-- Empty State -->
                <Border
                    Visibility="{Binding HasDownloadedFiles, Converter={StaticResource InverseBoolToVis}}"
                    Background="#121622"
                    BorderBrush="#1E2536"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="32"
                    Margin="0,4,0,0">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ui:SymbolIcon Symbol="ArrowDownload24" FontSize="36" Foreground="#475569" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="No downloaded wallpapers yet" FontSize="14" FontWeight="SemiBold" Foreground="#E2E8F0" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                        <TextBlock Text="Download wallpapers in original full resolution from Explore or Favorites to quick-set them here." FontSize="12" Foreground="#94A3B8" HorizontalAlignment="Center" TextAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Local Downloaded Items Grid -->
                <ItemsControl ItemsSource="{Binding LocalWallpapers}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="240" Height="135" CornerRadius="8" Background="#0F131D" BorderBrush="#222838" BorderThickness="1" Margin="0,0,8,8" ClipToBounds="True">
                                <Grid>
                                    <Image converters:AsyncImageLoader.ImageUrl="{Binding FilePath}" Stretch="UniformToFill" />

                                    <!-- Hover overlay -->
                                    <Border x:Name="Overlay" Background="#00000000" Opacity="0">
                                        <Grid VerticalAlignment="Bottom" Background="#990A0D14" Height="56">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Orientation="Vertical" Margin="8,6,12,6">
                                                <TextBlock Text="{Binding FileName}" FontSize="12" Foreground="#FFFFFF" TextTrimming="CharacterEllipsis" />
                                                <TextBlock Text="{Binding FormattedFileSize}" FontSize="11" Foreground="#9DA6B8" />
                                            </StackPanel>

                                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                                <Button Command="{Binding DataContext.SetAsWallpaperCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding FilePath}" Height="32" Width="36" Margin="6,0,0,0" Background="#0078D4" Foreground="White"><ui:SymbolIcon Symbol="Desktop24" FontSize="12"/></Button>
                                                <Button Command="{Binding DataContext.OpenFileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding FilePath}" Height="32" Width="36" Margin="6,0,0,0" Background="#1E2536" Foreground="#E2E8F0"><ui:SymbolIcon Symbol="Open24" FontSize="12"/></Button>
                                                <Button Command="{Binding DataContext.DeleteFileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Height="32" Width="36" Margin="6,0,0,0" Background="#1E2536" Foreground="#EF4444"><ui:SymbolIcon Symbol="Delete24" FontSize="12"/></Button>
                                            </StackPanel>
                                        </Grid>
                                    </Border>

                                    <!-- Hover animations -->
                                    <Border.Triggers>
                                        <EventTrigger RoutedEvent="MouseEnter">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetName="Overlay" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.18" />
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                        <EventTrigger RoutedEvent="MouseLeave">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetName="Overlay" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.18" />
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Border.Triggers>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
